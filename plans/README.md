# Implementation Plans

Generated by the improve skill on 2026-06-12 at commit `9f166b5`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

`LEARNING.md` in this folder is a beginner-friendly companion for the human
maintainer — agents don't need to read it, but must not delete it.

Filled metrics and scorecards stay in `notes/` (gitignored, local only). Commit
finished summaries to `docs/planning/` when ready (e.g. `improvement-report.md`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (Vitest + characterization tests + CI) | P1 | M | — | TODO |
| 002 | Next.js security bump + drop unused ElevenLabs SDK | P1 | S | — | TODO |
| 003 | Stop TTS playback on session unmount | P1 | S | — (001 recommended) | TODO |
| 004 | TTS contract fixes (speak completion, never speak English) | P1 | S | 001; after 003 | TODO |
| 005 | Structured JSON tutor output validated by Zod (v2 #2) | P2 | L | 001, 004 | TODO |
| 006 | Eval pipeline spike (v2 #3) | P2 | M | 001, 005 | TODO — **human-led**: the maintainer is building this one personally (see `LEARNING.md`); agents may assist with hints/reviews on request but must not implement it |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first.** It pins current parser behavior; 004 and 005 deliberately change that behavior and update/replace those tests — running them before 001 loses the before/after diagnostic (v2 todo item #0).
- **003 before 004** only to avoid two branches editing `useConversation.ts` simultaneously; no logical dependency.
- **004 before 005**: 005 preserves 004's "Spanish-or-empty, callers skip on empty" speech contract; landing 005 first would silently re-decide it.
- **005 before 006**: the eval's deterministic checks are written against `TutorResponseSchema`; building them against the legacy text parser is throwaway work.
- **002 is independent** and can land any time.

## Audited but not planned this run

(From the 2026-06-12 audit. Real findings the maintainer chose not to plan yet — do not re-audit; plan on request.)

- **Retry-inflated exchange count** — `useConversation.ts:25` counts correction retries as exchanges, skewing the header progress and the pacing number told to the LLM. Much easier after plan 005 (`responseType` becomes authoritative).
- **Double-correction drops the second retry from the UI** — `exchanges.ts:23-37`; characterized as a KNOWN QUIRK test in plan 001. Also easier after 005.
- **Word lookup**: context-blind cache key (`wordLookup.ts:10`) returns stale definitions for context-dependent words; missing `.catch` on the lookup promise (`ImmersiveDialogueView.tsx:76`) leaves the tooltip spinning on API failure.
- **Duplicated tutor pipeline** — `sendUserMessage` vs `handleRecordedAudio` in `useConversation.ts` repeat ~70 lines; consolidate after 005 settles the pipeline's final shape.
- **Dead code** — `useTTS.ts`, `useAudioPlayer.ts`, `ErrorToast.tsx` have zero importers; delete and remove the spec §9 note about it.

## Findings considered and rejected

- **OpenAI SDK v4 → v5+ migration**: no security pressure, working fine, blast radius (every `src/lib` API call) not worth it now.
- **`tutor.ts` module size**: fine at this repo's scale, and plan 005 reshapes it anyway.
- **localStorage BYOK keys / `dangerouslyAllowBrowser`**: documented MVP tradeoff (spec §9); superseded by v2 todo #1 (backend proxy), which needs its own design plan — note that `docs/agents/COMMON.md` currently lists "no backend proxy" as a non-negotiable and must be updated in lockstep when that work starts.
- **`onPointerLeave` ending the recording** (`MicButton.tsx:32`): by design — prevents a stuck mic when the pointer slides off the button.
- **Direction items D2 (backend proxy spike) and D4 (streaming TTS spike)**: presented 2026-06-12, not selected for planning this run.
